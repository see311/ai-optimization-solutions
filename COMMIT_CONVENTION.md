# AI Agent 提交规范

> 本文档供 AI Agent 读取并执行，规范文章提交到本仓库的完整流程。

## 仓库目录结构

```
ai-optimization-solutions/
├── docs/
│   ├── AI编程工具-Skills/         # AI 编程工具与 Skills 技能包
│   ├── Agent智能体设计/           # Agent 智能体架构与设计模式
│   ├── 记忆系统Memory/            # Agent 记忆系统与持久化方案
│   ├── RAG检索增强/               # RAG 检索增强与优化实战
│   ├── LLM网关架构/               # LLM 网关与生产级架构
│   └── AI工具/                    # AI 相关工具与开源项目
├── README.md
├── index.html
├── skill.md
└── COMMIT_CONVENTION.md           # 本文件
```

每篇文章的目录结构：
```
docs/{分类}/{文章名}/
├── 原文.md    # 原始文章内容
├── 总结.md    # 整理后的核心要点
└── 日期.md    # 上传日期，格式：YYYY-MI-DD
```

`日期.md` 内容示例：
```
2026-05-06
```
仅一行，纯日期文本，便于程序化读取。

## 提交流程

### 第 1 步：获取当前分类列表

通过 GitHub API 获取 `docs/` 目录下所有一级子目录，这些就是当前已有的分类：

```
GET /repos/{owner}/{repo}/contents/docs
```

响应中 `type === "dir"` 的条目即为分类。

### 第 2 步：检查文章是否已存在

在提交前，先检查目标文章是否已经存在于仓库中。

方法：递归获取 `docs/` 下的完整文件树：

```
GET /repos/{owner}/{repo}/git/trees/{branch}?recursive=1
```

检查是否存在与待提交文章同名（或高度相似）的目录。**如果已存在则停止提交**，向用户报告冲突，由用户决定是否覆盖/更新。

### 第 3 步：匹配分类

根据待提交文章的主题，判断应该归入哪个分类。匹配规则：

1. **精确匹配**：文章主题与已有分类名称语义一致 → 提交到该分类
2. **近似匹配**：文章主题与某分类有较强关联（如 RAG 相关文章归入 "RAG检索增强"）→ 提交到该分类
3. **无匹配**：文章主题不属于任何现有分类 → 新建分类

#### 分类匹配参考

| 关键词/主题 | 应归入分类 |
|------------|-----------|
| Skill、技能包、编程工具、开发工具 | AI编程工具-Skills |
| Agent、智能体、设计模式、Agentic | Agent智能体设计 |
| 记忆、Memory、持久化、知识图谱存储 | 记忆系统Memory |
| RAG、检索、检索增强、向量检索、Embedding | RAG检索增强 |
| 网关、Gateway、LLM架构、路由、降级 | LLM网关架构 |
| OCR、工具、开源项目、实用工具 | AI工具 |

> 以上仅为参考，Agent 应根据文章核心内容灵活判断，不局限于关键词匹配。

### 第 4 步：新建分类（如需要）

如果第 3 步判定需要新建分类：

1. 为分类取一个简洁明了的中文名称，格式参考现有分类（如"XXX系统YYY"、"AAA架构"等）
2. 在 `docs/{新分类名}/` 下创建文章目录
3. 分类名使用中文，不加空格，可包含英文和连字符

### 第 5 步：创建文章文件

在确定的分类目录下创建文章目录和文件：

```
docs/{分类}/{文章名}/原文.md
docs/{分类}/{文章名}/总结.md
docs/{分类}/{文章名}/日期.md
```

其中 `日期.md` 内容为上传日期，格式 `YYYY-MM-DD`，取提交当天的日期。

- **文章名**：简洁、能概括内容，中文为主，可用连字符连接，不使用空格
- **原文.md**：原始文章内容的完整 markdown
- **总结.md**：提炼后的核心要点，结构清晰，便于快速阅读。**必须在总结末尾标注生成本篇总结所使用的大模型名称**（如 GPT-4o、Claude 3.5 Sonnet、GLM-4 等），格式：`> 本总结由 {模型名称} 生成`

### 第 6 步：更新 README.md

提交文章后，需要同步更新 `README.md`：

1. 在对应分类的内容概览表格中添加新文章条目
2. 更新该分类的文章计数（如 "(3篇)" → "(4篇)"）
3. 如果是新建分类，在目录结构和内容概览中均添加该分类

### 第 7 步：更新 index.html

提交文章后，需要同步更新 `index.html`，在对应分类卡片中添加新文章的链接。

**文章条目格式**（插入到对应分类的 `<ul class="article-list">` 内，最后一个 `</li>` 之后）：

```html
<li class="article-item">
    <a href="docs/{URL编码的分类名}/{URL编码的文章名}/总结.md" class="article-link">
        <span class="article-icon">📝</span>
        <div class="article-info">
            <div class="article-name">文章名</div>
            <div class="article-desc">文章简介</div>
        </div>
    </a>
</li>
```

**更新要点**：
1. 找到 `index.html` 中对应分类的 `<ul class="article-list">` 区域
2. 在该区域最后一个 `</li>` 后插入新文章条目
3. 更新该分类卡片的 `<div class="category-count">` 中的文章计数（如 "3 篇文章" → "4 篇文章"）
4. 如果是新建分类，需要在 `index.html` 中新增一个完整的分类卡片（包含图标、标题、计数、文章列表），参照现有分类卡片的 HTML 结构
5. href 中的路径需要对中文和特殊字符进行 URL 编码（如空格 → `%20`，中文 → `%E4%B8%AD` 等）

### 第 8 步：提交

Commit message 格式：

```
docs: {分类} - {文章名}
```

示例：
- `docs: RAG检索增强 - 新型混合检索方案`
- `docs: Agent智能体设计 - Multi-Agent编排框架`

如果是新建分类，在 commit message 末尾标注：

```
docs: 新分类{分类名} - {文章名} [new-category]
```

## 检查清单

提交前逐项确认：

- [ ] 文章在仓库中不存在（同名或高度相似）
- [ ] 分类已确定（匹配已有 / 新建）
- [ ] 文章目录包含 `原文.md`、`总结.md` 和 `日期.md`
- [ ] `README.md` 已更新（文章条目 + 计数）
- [ ] `index.html` 已更新（文章条目 + 计数）
- [ ] Commit message 符合格式

### 第 9 步：提交后的检查

确认所有文件已正确提交后，可通过 GitHub Pages 或直接访问仓库验证页面显示是否正常。


**提交后验证**：每次提交完成后，必须检查 README.md 和 index.html 与实际目录结构完全一致，包括：

- 每个分类的文章计数必须与实际目录中的文章数一致
- 每篇文章在 README.md 和 index.html 中都有对应的在歉慁擴歎所有文章是否同时存在于 README.md 和 index.html 中
5. 确认所有文章的 href 路径正确且指向对应的 `总结.md` 文件

如果发现不一致，必须立即修复并提交更正。

## 异常处理

| 情况 | 处理方式 |
|------|---------|
| 文章已存在 | 停止提交，报告冲突，等待用户指示 |
| 分类归属不确定 | 列出 2-3 个候选分类，请用户确认 |
| 原文内容缺失 | 至少需要 `总结.md` 和 `日期.md`，`原文.md` 可后续补充 |
| API 限流 | 等待后重试，429 响应包含 `x-ratelimit-reset` |
